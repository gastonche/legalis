import { INGESTION_MILESTONE } from "./index";

// Placeholder CLI. Milestone 2 wires: read corpus/ → chunk → embed (Workers AI) → Vectorize upsert.
console.log(`[ingestion] pipeline lands in milestone ${INGESTION_MILESTONE}.`);
