/**
 * Corpus ingestion (milestone 2): language/structure-aware chunking that
 * preserves article/section numbers, per-chunk metadata, Workers AI embeddings,
 * and Vectorize upsert. Implementation lands in M2.
 */
export const INGESTION_MILESTONE = 2 as const;
